<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% Helpers.is_logged_in?(session) ? @user = Helpers.current_user(session) : @user = {username: "XXXXX", balance: "XXXXX"} %>
    <h1>Welcome <%= @user.username %></h1>
    <a href="/logout">Log Out</a>
    <h3>Your Balance: <%= @user.balance.to_f %></h3>

  </body>
</html>
